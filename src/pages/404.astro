---
import Layout from "@layouts/Layout.astro";
import { localizePath } from "astro-i18next";
import { changeLanguage } from "i18next";

changeLanguage("zh");

---

<Layout title="404">
	<section class="flex items-center h-full p-16">
		<div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
			<div class="max-w-md text-center">
				<h2 class="mb-8 font-bold text-9xl">
					<span class="sr-only">Error</span>
					<span class="text-primary">404</span>
				</h2>
				<p class="text-3xl font-semibold md:text-3xl">对不起，我们找不到这一页。</p>
				<p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">但别担心，你可以在我们的主页上找到很多其他东西。</p>
				<a rel="noopener noreferrer" href={localizePath("/")} class="btn ml-4">回到主页</a>
			</div>
		</div>
	</section>
</Layout>
